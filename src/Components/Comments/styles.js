import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  commentsLists: {
    padding: 10,
    marginTop: 10,
  },
  commentInput: {
    margin: '20px 0 10px',
  },
});
