import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  table: {
    minWidth: '100%',
  },
  tableHead: {
    color: '#E6E6E6',
    backgroundColor: '#800300',
    fontSize: '16px',
    fontWeight: 600,
  },
});
