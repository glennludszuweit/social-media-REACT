import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  textField: {
    margin: 20,
    position: 'relative',
  },
  close: {
    position: 'absolute',
    right: 4,
  },
});
